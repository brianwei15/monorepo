start:
  class: uav.autonomous_modes.TakeoffMode
  params:
    takeoff_type: vertical
  transitions:
    complete: GPS

GPS: 
  class: uav.autonomous_modes.NavGPSMode
  params:
    coordinates: '[((0, 0, -5), 100, "LOCAL")]'
  transitions:
    complete: land

land:
  class: uav.autonomous_modes.LandingMode